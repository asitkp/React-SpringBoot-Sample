import React, { Component } from "react";
import PropTypes from "prop-types";

class UpdateCourse extends Component {
  static propTypes = {};

  render() {
    return <div>Updating course..</div>;
  }
}

export default UpdateCourse;
